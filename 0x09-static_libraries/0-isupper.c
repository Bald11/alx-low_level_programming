#include "main.h"
#include <ctype.h>

/**
 * _isupper - the prototype of isupper
 * @c: integer passed
 * Return: 1 if uppercase, 0 otherwise
 */

int _isupper(int c)
{
if (isupper(c))
return (1);
else
return (0);
}
