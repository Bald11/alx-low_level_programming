#include <string.h>

/**
 * _strstr - locates a substring
 * @haystack: char to pointer
 * @needle: char to pointer
 * Return: returns strstr
 */

char *_strstr(char *haystack, char *needle)
{
return (strstr(haystack, needle));
}
