#include <string.h>

/**
 * _strpbrk - searches a string for smth
 * @s: char to pointer
 * @accept: char to pointer
 * Return: returns strpbrk
 */

char *_strpbrk(char *s, char *accept)
{
return (strpbrk(s, accept));
}
