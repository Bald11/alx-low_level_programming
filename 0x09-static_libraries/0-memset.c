#include <string.h>

/**
 * _memset - i have no clue
 * @s: char to pointer
 * @b: char
 * @n: integer
 * Return: return memset
 */

char *_memset(char *s, char b, unsigned int n)
{
return (memset(s, b, n));
}
