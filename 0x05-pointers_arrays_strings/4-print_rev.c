#include <string.h>
#include "main.h"

/**
 * print_rev - a function that outputs a reversed the input
 * @s: a character
 * Return: void
 */

void print_rev(char *s)
{
int x = strlen(s);
for (int = x - 1; i > 0; i--)
{
printf("%c", str[i]);
}
printf("\n");
}
