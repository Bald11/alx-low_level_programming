#include <string.h>

/**
 * _memcpy - I don't really know
 * @dest: char to pointer
 * @src: char to pointer
 * @n: integer
 * Return: return memcpy
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
return (memcpy(dest, src, n));
}
