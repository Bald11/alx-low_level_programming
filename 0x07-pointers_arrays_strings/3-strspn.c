#include <string.h>

/**
 * _strspn - dunno tbh
 * @s: char to pointer
 * @accept: char to pointer
 * Return: returns the result of strspn
 */

unsigned int _strspn(char *s, char *accept)
{
return (strspn(s, accept));
}
